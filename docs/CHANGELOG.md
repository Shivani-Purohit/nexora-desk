# Changelog

All notable changes to this project are documented here, in reverse chronological order.

## Phase 0 — Environment and repository readiness

- Initialized Git repository and pnpm workspace skeleton
- Added root TypeScript strict base config, ESLint (flat config), Prettier, commitlint/husky
- Added Docker Compose stack for PostgreSQL 18, Redis 8, MinIO, Mailpit
- Expanded `.gitignore` for local overrides, IDE folders, key files
